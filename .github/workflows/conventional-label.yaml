name: Label PR

on:
  pull_request:
    types: [opened, edited, synchronize, reopened]

jobs:
  label:
    uses: OpenVoiceOS/gh-automations/.github/workflows/conventional-label.yml@master
    secrets: inherit
